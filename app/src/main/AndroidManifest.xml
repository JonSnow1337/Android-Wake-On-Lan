<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.teamgy.wakeonlan" >
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />


    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" >
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EditPCActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustResize" >
        android:parentActivityName=".MainActivity">
            >
        </activity>
        <activity android:name=".SQL.AndroidDatabaseManager"
            android:theme="@style/Theme.AppCompat.Light"/>


        <activity android:name=".SettingsActivity">
            >
        </activity>
        <service android:name=".WOLService"
            android:enabled="true"
            android:exported="false"
            >
        </service>
        <receiver android:name="WakeOnHomeWifiReciever"
            android:enabled="true"
            android:exported="true">
            <intent-filter >
                <action android:name="android.net.wifi.STATE_CHANGE"></action>
            </intent-filter>

        </receiver>
    </application>

</manifest>
